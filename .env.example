# FogSift Environment Variables
# Copy this file to .env for local development
# For production, set these in Cloudflare Pages Environment Variables

# ============================================
# Ko-fi Integration (Required for Queue)
# ============================================

# Ko-fi webhook verification token
# Get this from your Ko-fi webhook settings page
KOFI_VERIFICATION_TOKEN=your_kofi_verification_token_here

# ============================================
# Cloudflare KV (Required for Queue Persistence)
# ============================================

# KV namespace binding for queue storage
# In production: Set binding name to QUEUE_KV in Cloudflare Pages
# In local dev: Use wrangler.toml kv_namespaces configuration
# QUEUE_KV is bound automatically via wrangler.toml

# ============================================
# Optional: Analytics & Monitoring
# ============================================

# Google Analytics (if using)
# GA_TRACKING_ID=G-XXXXXXXXXX

# Cloudflare Web Analytics (free, privacy-focused)
# Enabled automatically via Cloudflare Pages dashboard

# ============================================
# Development Environment
# ============================================

# Node environment
NODE_ENV=development

# Port for local dev server (default: 5050)
# PORT=5050

# ============================================
# Notes
# ============================================

# 1. Never commit .env to git (already in .gitignore)
# 2. For Cloudflare deployment, set these in Pages dashboard:
#    Settings > Environment variables
# 3. Ko-fi webhook endpoint: https://fogsift.com/api/webhook/kofi
# 4. Queue API endpoint: https://fogsift.com/api/queue
