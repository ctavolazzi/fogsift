# FogSift Status

Quick repository status check.

## Steps
1. Run `date`.
2. Check git status in repo (`git status -s`).
3. List active work efforts in `_work_efforts/` and read the latest devlog entries.
4. Summarize current state, active work, and next step.
